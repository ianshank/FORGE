# v0.5 loop survival — code path closed, live measurement pending

**Status:** Slices 1–2 of the self-improving loop are in tree.
Live evidential capture is **not** claimed. This environment has no
Docker daemon and no Paper/Minecraft server, so a Rust-runner capture
against live MC cannot run here. Committed baseline snapshots still
record `evidential_episodes = 0`.

This file is operator notes plus a test citation. It is **not** a
benchmark snapshot (Markdown under `docs/results/` is not indexed in
`INDEX.toml`). Do not treat it as trained-vs-random evidence.

## What is proven without live Minecraft

| Claim | Where it is pinned |
|---|---|
| Self-play identity is `FORGE_MC_RANDOM_ACTIONS=false` + `FEATURES=mc-live-bundled`; shipped `runner.toml` stays `random_actions = true` | `crates/forge-mc-runner` env-ladder tests; `tests/python/integration/test_mc_self_play_unit.py` |
| Mid-episode `RECONNECTING`/`BUSY` discards the partial trajectory and continues `Runner::run`; next Reset is a new MDP | `mid_episode_reconnecting_discards_and_continues_run` in `crates/forge-mc-runner/tests/mc_runner_integration.rs` |
| Consecutive transient failures hit the config cap and fail the run | `consecutive_reconnecting_hits_cap_and_fails_run` |
| Mid-episode `INTERNAL` still fails the run | `mid_episode_protocol_error_surfaces_as_runner_env_error` (untouched) |
| Bot `isHealthy` tracks advancing mineflayer tick age, not WS think-time | `mc-bot/test/bot_manager.test.ts` |
| Nested milestone/crafting **file contents** fold into `schema_id`; missing nested files fail closed | `xlang_shipped_rewards_schema_id_folds_nested_files` (Rust / JS / Python) |
| `block_embeddings.toml` is a separate obs-layout pin, not today's two-input `schema_id` | `xlang_block_embeddings_pinned_to_known_good` |

Reconnect survival was re-run in this change (`cargo test -p forge-mc-runner --test mc_runner_integration reconnect`). Those tests drive a real `MinecraftEnv` against `MockBot` over the production WebSocket protocol. They prove the **runner** contract. They are not live Paper/MC episodes.

## What is not proven

- No new live Minecraft episodes. Existing committed snapshots
  (`docs/results/v0.5-first-real-run-baseline.json` and `-v2.json`)
  still declare `evidential_episodes = 0`.
- No trained 1-episode smoke against `mc-live-bundled`. This VM has
  neither Docker nor `torch`/`onnx`.
- `docs/results/v0.5-trained-vs-random.md` remains a **template**.
  Filling it requires both variants to clear the plotter floor of
  `MIN_EVIDENTIAL_EPISODES_FOR_COMPARISON = 3`
  (`scripts/mc_plot_baseline.py`).

Python `scripts/v05_manual_baseline.py` already ends episodes on
`RECONNECTING`/`BUSY`/`INTERNAL`. That path cannot prove Rust
`Runner::run` survival; the MockBot integration tests above do.

## Remaining ops (live host with Docker)

Short N, then trained 1-ep, then scale. Prefer the **Rust runner**.

1. Random evidential capture (`mc-live`, `--baseline-only` /
   `FORGE_MC_RANDOM_ACTIONS` unset so shipped `random_actions = true`
   wins):

   ```bash
   scripts/mc_self_play.sh --baseline-only
   python -m forge.training.muzero_mc.cli capture-baseline \
     --variant random --episodes 8 --out docs/results/v0.5-random-evidential.json
   ```

2. Trained 1-ep smoke (`RUNNER_FEATURES=mc-live-bundled`,
   `FORGE_MC_RANDOM_ACTIONS=false`):

   ```bash
   scripts/mc_self_play.sh
   python -m forge.training.muzero_mc.cli capture-baseline \
     --variant trained --episodes 1 --out docs/results/v0.5-trained-1ep-smoke.json
   ```

3. Scale each variant until evidential episodes ≥ 3. Commit the
   snapshots + `INDEX.toml` sha256. Only then populate
   `docs/results/v0.5-trained-vs-random.md`. Do not commit a
   declaration with `evidential_episodes >= 3` (declarations are for
   zero-evidential historical records only).
