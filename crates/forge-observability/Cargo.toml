[package]
name = "forge-observability"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
keywords.workspace = true
categories.workspace = true
description = "Shared tracing/log initialization for FORGE binaries — env-driven text/JSON formatting"

[dependencies]
tracing = { workspace = true }
# Inherits the workspace features ("env-filter", "json") so JSON output needs no
# extra feature plumbing and the whole workspace shares one subscriber version.
tracing-subscriber = { workspace = true }
